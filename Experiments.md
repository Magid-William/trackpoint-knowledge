| Experiment | Status | Hypothesis | Result |
|------------|--------|------------|--------|
| [Exp01](experiments/Exp01/Exp01.md) | Complete | ZMK shield with PMW3610 driver builds & flashes on NiceNano V2 | Success: builds, flashes headless, shell works, all devices READY |
| [Exp02](experiments/Exp02/Exp02.md) | Failed | SPI handshake with Pro Mini PMW3610 emulator | AVR ISR timing: nRF52 SPIM byte gap too small; motion data corrupted by byte-shift. Fix must be on master side. |
| [Exp03](experiments/Exp03/Exp03.md) | Failed | 1-byte shift compensation (AVR-only, no driver changes) | AVR SPDR is single-buffered for RX; received byte overwrites TX data at byte boundary. With zero inter-byte gap, next byte loads the received byte (0xFF) instead of burst data. Fix requires RP2040 or per-byte transactions (driver change). |
| [Exp04](experiments/Exp04/Exp04.md) | Success | Per-byte SPI transactions — `pmw3610_read()` split into 1-byte transactions with CS deassertion between each, giving AVR time to prepare response | No shift! Burst hex dump shows correct X_L/Y_L/XY_H at buf[1-3]. Circle pattern values cycle correctly. All builds pass. |
| [Exp05](experiments/Exp05/Exp05.md) | Success | Rectangle speed test with 4 speeds at 20 Hz → 100 Hz MOT; SPI polling → interrupt-driven | Smooth at 100 Hz! Polling SPIF missed bytes during Serial.print (stops after 30s). Fixed with SPI_STC_vect ISR. Ran 30+ min steady. Double success. |
| [Exp06](experiments/Exp06/Exp06.md) | Complete | Verify TrackPoint PS/2 communication via serial monitor before integrating with SPI pipeline | Success! IC streams on power-up (no init needed), rejects host commands. 500+ packets captured cleanly over 30s. D7=CLK, D3=DAT confirmed working at 3.3V with internal pull-ups. |
| [Exp07](experiments/Exp07/Exp07.md) | Complete | Merge PS/2 reader (Exp06) into SPI pipeline (Exp04/Exp05) — real TrackPoint motion through to ZMK | Success! Cursor moves with nub. Fixes applied: X negation, sensitivity scaling (x/4,y/4), burst cleared after MOT to eliminate stale repeats. All builds pass. |
| [Exp08](experiments/Exp08/Exp08.md) | Success | Power curve smoothing: activity ramp replaces flat `/4` so cursor starts slow then accelerates to normal during sustained motion | 1px precision at low end, 6× speed at peak. Pro Mini `x*ramp*3/40` + ZMK `zip_xy_scaler 1/4`. |
| [Exp09](experiments/Exp09/Exp09.md) | Success | Wiring update per AGENTS.md: MOT 2→14, PS/2 CLK 7→3, DAT 3→2, add NPN=D4 and TOUCH=D19 | All builds pass, flashed both boards, SPI handshake confirmed, cursor moves. Physical rewiring required for old setups. |
| [Exp10](experiments/Exp10/Exp10.md) | Success | Deep sleep on PS/2 idle + TTP223 INT0 wakeup (D2). Move TTP223 D19→D2, PS/2 DAT D2→D7. D13 LED off during sleep. | Sleep/wake cycles stable. TTP223 touch wakes MCU, cursor resumes instantly. SPI kept enabled, TrackPoint powered throughout. |
| [Exp11](experiments/Exp11/Exp11.md) | Failed | XIAO BLE central dongle (USB HID) + NiceNano peripheral (BLE split) eliminates cursor lag vs direct BLE to PC | Mouse stops after ~1-2s. Queue/stack increases help lifetime but don't fix root cause. Forking ZMK for queued notifications also ineffective. Root cause unknown — needs JTAG debugging. |
| [Exp12](experiments/Exp12/Exp12.md) | Partial | Buffer pool expansion + report interval throttle (config-only) fixes dongle stall | All 4 builds pass. Peripheral .conf has report throttle (20ms), BT_L2CAP_TX_BUF_COUNT=10, thread-monitoring Kconfigs. Ready for flash & test. |
| [Exp13](experiments/Exp13/Exp13.md) | Complete | Standalone CH340G USB TTL programmer replaces Arduino Nano passthrough for flashing Pro Mini | Success: CH340G detected (COM1, busid 1-3), blink-test compiled with Alpine avr-gcc, 234 bytes flashed & verified via avrdude. AGENTS.md updated. |
| [Exp14](experiments/Exp14/Exp14.md) | Partial | Dual rail power switching (D4=NPN GND, D6=P-MOSFET VCC) + PS/2 pin float during sleep kills all parasitic power | Firmware runs (D13 blinks), but TrackPoint LED stays off — suspected hardware wiring issue. CH340G unstable after usbipd cycles, serial unreliable. |
| [Exp15](experiments/Exp15/Exp15.md) | Success | Auto layer-toggle on trackpoint motion: PMW3610 driver activates mouse layer (2) while moving, deactivates after 2s idle | Layer activates on motion, deactivates after 2s idle. Fixes: remove Kconfig depends, fix API 2-arg signature, remove stale-event guard from deactivation. |
| [Exp16](experiments/Exp16/Exp16.md) | Abandoned | Exp05 synthetic rectangle over BLE — 9 sub-iterations testing MOT rates, smoothing, driver accel, and badjeff's input processors. None matched `&mmv` smoothness. BLE connection interval is the root bottleneck and can't be tuned via Kconfig. | Final config: stock driver + `&zip_ble_report_rate_limit 4` + `CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y` — untested. |
| [Exp17](experiments/Exp17/Exp17.md) | **Success** | I2C handshake sanity: Pro Mini I2C slave (0x42) + ZMK I2C master via new `promini,trackpoint-i2c` driver | I2C reads `reg[0x00]=0x3E` via 100ms polling. Reliable, zero errors. MOT interrupt on nRF has config issue (polling bypasses it). |
| [Exp18](experiments/Exp18/Exp18.md) | **Success** | Synthetic rectangle motion over I2C: Pro Mini rectangle sequencer + ZMK I2C driver with `input_report` moves cursor in a rectangle at varying speeds | Cursor traces rectangle with visible speed variants: 0.5× right, 4× down, 1× left/up. Fixes: raw int8 encoding (dropped 12-bit), Y negation in driver, skip-based dwell for slow segments. |
| [Exp19](experiments/Exp19/Exp19.md) | **Success** | Real PS/2 TrackPoint motion over I2C: Pro Mini reads PS/2 data, exposes X/Y via I2C burst with MOT pin signaling and 50Hz read gating | Cursor moves smoothly with zero drift. Key findings: power-cycling TrackPoint causes drift (keep powered), one-shot calibration beats EMA baseline, MCU-only sleep via TTP223 wake works. |
| [Exp20](experiments/Exp20/Exp20.md) | **Success** | Ditch TTP223; use CLK-inhibit + WDT wake instead. Hold CLK LOW during sleep, WDT poll every 60ms to check for non-zero motion. Also ported layer-toggle from SPI to I2C driver. | TTP223 removed, CLK-inhibit sleep + WDT 60ms verified. Layer-toggle ported to I2C driver (activation on motion, deactivation after 2s idle). Both Pro Mini and ZMK firmware flashed. I2C bus scan confirms device 0x42 alive. |
| [Exp21](experiments/Exp21/Exp21.md) | **Complete** | NiceNano asserts Pro Mini RST via GPIO before I2C init to guarantee a clean boot state — no stale sleep, no missed probe. | SW complete: driver pulses reset (100ms LOW), CI build passed, NiceNano flashed. Needs physical wire P0.08→1kΩ→Pro Mini RST to verify. |
| [Exp22](experiments/Exp22/Exp22.md) | **Partial** | BLE dongle with XIAO nRF52840 over I2C split transport. Dual-mode right half (standalone/peripheral). | Standalone fixed: cursor moves. Needed Exp21 driver (combined i2c), always-enable listener, remove input-processor. Dongle blocked: XIAO USB unreliable. Key: never use split I2C with AVR TWI; always build with USB logging. |
| [Exp23](experiments/Exp23/Exp23.md) | **Success** | Pin ZMK revision to working SHA (July 26) to eliminate regression variable. Cursor stopped moving after ZMK main advanced. | Standalone cursor moves with pinned ZMK. Confirmed: ZMK main carries regressions. Permanently pin ZMK to the working SHA. |
| [Exp24](experiments/Exp24/Exp24.md) | **Complete** | BLE dongle with XIAO nRF52840 (I2C split transport). Fix peripheral build by restructuring devicetree files. | Devicetree restructured — split_inputs/listener moved from .dtsi to .overlay. All builds pass. |
| [Exp25](experiments/Exp25/Exp25.md) | **Complete** | Snapshot of working dongle topology — no code changes. Tag the verified pin config and firmware versions. | All builds pass. Dongle mode working: cursor moves via XIAO USB HID. Standalone mode not retested. |
| [Exp26](experiments/Exp26/Exp26.md) | **Success** | Correct PS/2 pins: TrackPoint DAT→D7 (turned out to be D3), CLK→D3 (turned out to be D7). I2C pins already correct in Exp22 overlay (P0.17/SDA, P0.20/SCL, P0.06/MOT, P0.08/RST). | Exp22 base + Promini Exp25 + pin swap + ZMK pinned SHA + standalone build only. Cursor moves. Extensive logging added to driver. |
| [Exp27](experiments/Exp27/Exp27.md) | **Success** | Dongle topology with XIAO nRF52840 BLE central + NiceNano peripheral + Pro Mini I2C slave. | All 5 builds pass. Peripheral + dongle both flashed and paired. Cursor moves via XIAO USB HID. No XIAO USB reliability issues this time. |
| [Exp28](experiments/Exp28/Exp28.md) | **Success** | Migrate Exp27 trackpoint config into dabase repo (zmk-config-dabaseV_0-2) with 2 peripherals + dongle. | All 7 builds pass. Left/right/dongle flash and pair. Keys send. Trackpoint moves after Pro Mini reset. Sticky keys identified as I2C timeout against sleeping Pro Mini — fix: wire P0.08 → Pro Mini RST. |
| [Exp29](experiments/Exp29/Exp29.md) | **Success** | Layer-toggle via dongle temp_layer input processor + DT-configurable swap/invert axes. | All 8 builds pass (dabase config). Layer toggle works — `tp_layer` activates on touch, 2s idle timeout. `swap-xy`, `invert-x`, `invert-y` exposed as DT booleans (default false). Driver had one-time change to replace hardcoded `#define`s with DT properties. |
| [Exp30](experiments/Exp30/Exp30.md) | **Failed** | DT divider in ZMK driver was choppy. Move divider to Pro Mini: per-sample division at 50 Hz with remainder tracking. | Pro Mini-side `SPEED_DIVIDER 2` causes cursor jitter — half-speed reports oscillate instead of smooth deltas. Driver-side divider stays removed; scaling needs a different approach. |
| [Exp31](experiments/Exp31/Exp31.md) | **Success** | I2C polling against sleeping Pro Mini wasted CPU, starving keyboard scan. Fix: exponential backoff on consecutive errors (10ms → 100ms → 1s → 5s). | Pure ZMK driver fix. `consecutive_errors` counter in `trackpoint-i2c.c` backs off poll interval. Resets to 10ms instantly on first successful read. No Pro Mini changes needed. |
| [Exp32](experiments/Exp32/Exp32.md) | **Success** | Hold J on `tp_layer` to scroll: `&zip_xy_to_scroll_mapper` via layer-specific input-processor override when layer 6 active. | Scroll works! Speed 0.2× via `&zip_scroll_scaler 1 5`. Y-axis inverted via `&zip_scroll_transform INPUT_TRANSFORM_Y_INVERT`. All overlay-only, zero driver changes. |
| [Exp33](experiments/Exp33/Exp33.md) | **Failed** | Per-axis dead zone in Pro Mini firmware zeroes small deltas (±3) to eliminate cursor drift from trackpoint sensitivity. | Dead zone too blunt — blocks small movements or lets drift through. Needs a different approach (drift compensation, not hard cutoff). Reverted. |
| [Exp34](experiments/Exp34/Exp34.md) | Complete | Diagnose permanent I2C lockup after 5-15 min. Cap `consecutive_errors` at 200 to prevent uint8_t wrap + 10ms flood burst. Pro Mini adds wake_count, D13 heartbeat, reg 0x01 for ZMK liveness probe. | Superseded by Exp36: polling a sleeping slave is the root problem; Exp36 eliminates polling during sleep entirely (MOT level gate). |
| [Exp35](experiments/Exp35/Exp35.md) | **Success** | Clean up dead code (NPN/PMOS gates, calibration, LED, unused library API) + make deep sleep optional (default off). | Pro Mini firmware 20% leaner (4766 bytes vs 5972), no behavioral changes. Sleep controlled by `SLEEP_ENABLED` flag. |
| [Exp36](experiments/Exp36/Exp36.md) | **Success** | Level-based MOT sleep gate: MOT LOW during sleep, HIGH awake. ZMK driver cancels poll on sleep edge (zero I2C traffic), resumes on wake edge (~100ms vs 5s backoff). Central-only build in zmk-trackpoint-shield (no dongle). | Cursor moves, sleep engages (TWI off, zero I2C traffic), wake resumes instantly. No lockup after 18 min idle (Exp34 killer gone). Two bugs fixed: `ZMK_SPLIT_ROLE_CENTRAL` undefined in non-split builds (overlay silently used split branch), temp_layer needs `/{}` root block. Known-good: promini `bb2632f`, driver `58fd284`, shield `0949120`. Trackpoint stays powered (wake source) — power-cut deferred. |
| [Exp37](experiments/Exp37/Exp37.md) | **Success** | Verify I2C connection restoration on Pro Mini wake — no code changes unless a gap is found. | Zero code changes needed. First post-wake I2C read succeeds every cycle (`Wire.begin()` before MOT HIGH — ordering correct). Repeated sleep/wake cycles stable, speed_scale persists, long-idle wake clean. Next: true POWER_DOWN deep sleep. |
| [Exp38](experiments/Exp38/Exp38.md) | **Success** | Minimal serial logging — wake/sleep transitions only, `SERIAL_LOG` as compile-time config. | Serial shows exactly `Sleeping...` / `Woke!`. Banner, SLP heartbeat, and motion coords removed. Binary 5724→5202 bytes. `SERIAL_LOG 0` builds fully silent (no UART). reg 0x01 wake_count intact. |
| [Exp39](experiments/Exp39/Exp39.md) | **Failed** | Flash ATtiny85 via Arduino Leonardo ISP (ArduinoISP), then bench-test trackpoint slave. | Mouse only moved on one axis — other axis produced no motion; cause uninvestigated. |
| [Exp40](experiments/Exp40/Exp40.md) | **Success** | Flash ATtiny85 blink via Arduino Nano ISP (ArduinoISP) at 3.3V, PlatformIO + WSL avrdude. | Signature 0x1e930b, 466 bytes written + verified, LED blinks on PB0. Windows CH340 driver broken (win32 error 31, survives reboot) — flashing via WSL Linux driver required. |
| [Exp41](experiments/Exp41/Exp41.md) | **Success** | Port trackpoint-i2c-slave (Exp38) to ATtiny85 — compile only. 4 adaptations: pins, SERIAL_LOG 0, WDTCR, Wire.end(). USI-TWI on PB0/PB2 supports combined i2c_write_read_dt. | Green `pio run`. Flash 3318/8192 (40.5%), RAM 94/512 (18.4%). PS2Trackpoint compiled unchanged. Next: flash via Nano ISP + bench test. |
| [Exp42](experiments/Exp42/Exp42.md) | **Success** | ATtiny85 isolation test: SSD1306 0.91" OLED (Tiny4kOLED 2.3.0) as USI I2C **master** on PB0/PB2 shows live PS/2 X/Y; reuses WDT idle sleep. Standalone, no NiceNano. | Green `pio run`, no warnings. Flash 5636/8192 (68.8%), RAM 125/512 (24.4%). Slave build unaffected (3318/94). Bench checklist pending hardware. |
| [Exp44](experiments/Exp44/Exp44.md) | **Success** | I2C slave with sleep compiled out — `SLEEP_ENABLED=0` one-liner to remove WDT sleep / TWI shutdown / CLK-inhibit as a variable. | Build success (run 30832316296, 30s), 4512 B written + verified (690 B leaner than Exp38). Firmware live, never sleeps. Bench validated, no issues. Repo moved to `Magid-William/promini-trackpoint`. |
| [Exp46](experiments/Exp46/Exp46.md) | **Success** | Prove serial monitor path: bare `serial-test` (millis println) + `ps2-bytes` raw PS/2 byte reader, both at 9600 via dedicated CH340G. | Serial proven: clean millis timestamps every 500ms. Raw TrackPoint stream captured (0xFF idle + status/data bytes) on CLK=D7, DAT=D3. Cause of earlier garbage = burned Arduino Nano (overheated, wire fault) + wrong baud (115200→9600). Library `readPacket` still prints no X/Y — parse/sync issue next. |
| [Exp47](experiments/Exp47/Exp47.md) | **Success** | 4-wire I2C sanity test: Pro Mini ↔ NiceNano on SDA/SCL/GND/VCC only — ditch MOT (P0.06) and RST (P0.08). Driver makes irq-gpios/reset-gpios optional; never-sleeping slave (SLEEP_ENABLED 0). | Mouse moves with zero GPIO wires. `i2c scan` finds 0x42, burst reg 0x12 returns X/Y. Driver boots with `no irq-gpios — plain polling` path, no errors. P0.06/P0.08 free. USB log output silent on this build (unexplained, non-blocking). Flashing reconfirmed hands-free: user never pressed Pro Mini reset (DTR pulses on port open, stk500v1 syncs inside the window). |
| [Exp48](experiments/Exp48/Exp48.md) | **Success** | NPN+PNP power gates from NiceNano: P0.06 NPN low-side GND cut, P0.08 PNP high-side VCC cut (10k bases), driven by `zmk,ext-power-generic`. Two builds (poweron/poweroff) + DOT key = `&ext_power EP_TOG`. | Full cycle proven: poweron → 0x42 found + mouse moves; poweroff → 0 devices, pins float, Pro Mini dead; DOT toggle → 0/1 devices live. Key findings: ext-power driver only supports `DT_INST(0)` and nice_nano v2 already owns it (EXT_POWER node, P0.13 rail switch) → fix = node-name merge in `/ {}`; transistor E/C swap blocks current (swap = works). Driver log flood → edge-triggered link loss/restore logs (stated exception). `flash-nicenano.ps1` hardened (drive disconnect after copy = flash accepted). |
| [Exp49](experiments/Exp49/Exp49.md) | **Success** | ZMK deep sleep (`CONFIG_ZMK_SLEEP=y`, 120s timeout) auto-cuts Pro Mini power via PM suspend of ext_power (P0.06/P0.08); keys-only wake via kscan0 wakeup-source; trackpoint never a wake source. | Zero driver/Promini changes — two cmake-args on poweron build. Verified: USB unplugged → idle 2min → Pro Mini dies; trackpoint touch does NOT wake; key press wakes, Pro Mini up in ~1s, pointer resumes. No lockups across cycles. Wishlist "auto-cut on inactivity" done. |
| [Exp50](experiments/Exp50/Exp50.md) | **Success** | Port Exp49 sleep/wake to dabase_v2 right shield — same ext_power gates + ZMK sleep, first sleep test in split-peripheral topology (dongle = central). Left half matched after. | Battery: 40s idle → deep sleep (Pro Mini power cut), touch does NOT wake, **any key wakes**, pointer resumes ~1s, cycles stable. Timeout 30s → 15min. Left half: 40s idle → sleeps, any key wakes, encoder doesn't wake — also 15min (`442b701`). Soft-off was a wrong proxy (it kills all wakeups by design) — dropped softoff build + wakeup-sources node. Wake mechanism: kscan0 `wakeup-source` → physical_layouts sets WS_ENABLED at boot → suspend loop skips kscan → GPIO DETECT wakes System OFF. |
| [Exp51](experiments/Exp51/Exp51.md) | **Success** | Hold K → trackpoint tilt becomes volume, very slow (slower than scroll). `&lt 7 K` layer-tap + te9no keybind processor on layer 7 (replaces reserved `extra1`). Zero scaler guards keep cursor put since keybind STOP can't be honored through `process-next`. | User-verified: hold K→tilt → volume steps very slowly (≈3× slower than scroll), cursor stays put; tap K still types K; scroll (J) unchanged. Fixed RC row/col vs keycode macro collision in dongle overlay (include order). Zero pick-up via `volume_move_zero` (`zip_xy_scaler 0 1`). Vol direction Y-invert = up louder (`be1b088`). |
| [Exp52](experiments/Exp52/Exp52.md) | **Success** | Fresh ATtiny85 revisit — flash a bare serial-print loop (`Serial.println(millis())` @9600) via Nano ArduinoISP + WSL avrdude, read it back through the CH340G. No trackpoint, no ZMK. | Clean `millis()` heartbeat every ~500ms on CH340G @9600. Build 1240 B/79 B RAM. Key: tiny-core `Serial` = soft-serial TX on PB0 (pin 5); `main.cpp` needs `#include <Arduino.h>`; drain port (`timeout 2 cat`) before avrdude; CH340G now busid 3-1. |
| [Exp53](experiments/Exp53/Exp53.md) | **Success** | Repeat Exp52 flash, but read the ATtiny85 serial through the Leonardo instead of the CH340G: Leonardo reflashed from ArduinoISP to a SoftwareSerial forwarder (RX=D16 = ATtiny85 pin 5) bridging to USB CDC. Zero rewiring. | Flashed ATtiny85 via Leonardo ISP (sig 0x1e930b, 1240 B verified), then read clean `millis()` heartbeat (~514ms) on Windows COM9 over the Leonardo's native USB. One USB cable, no CH340G, no WSL for reading. Gotcha: 0xffffff if RESET jumper missed; Leonardo can't be ArduinoISP + serial bridge at once (reflash ~7s each way). |
| [Exp54](experiments/Exp54/Exp54.md) | **Success** | ATtiny85 PS/2 serial reader (X/Y only) flashed via Leonardo ISP, serial read back through the Leonardo bridge — real trackpoint motion, no I2C slave. | Flashed 2270 B (sig 0x1e930b, verified) via Leonardo ISP, read live X/Y @9600 through the Leonardo bridge. Both axes move (X −19..+21, Y −29..+11), no Exp39 one-axis regression. Trackpoint at 5V worked. Live-stream PowerShell command documented in Exp54. |
| [Exp56](experiments/Exp56/Exp56.md) | **Success** | ATtiny85 PS/2 → I2C slave @0x42 → stock `promini,trackpoint-i2c` driver read from ZMK shell. Read-only test, no host cursor. | Combined sketch reflashed (sig 0x1e930b, 2440 B, no `-D`), moved to NiceNano PCB, trackpoint rewired carefully at 3.3V (CLK/DAT verified, RST float). Live x/y streams from shell: `0x03` debug `[status,xraw,yraw,to_lo,to_hi]` + `0x12` destructive burst both show real nub motion, both axes, no regression. Exp56 3.3V silence was wiring, not voltage. Added `live-trackpoint.ps1` (busy-loop live streamer, 212 samples/8s verified). Driver USB log still silent (Exp47 quirk) — shell `i2c read` is the path. |
| [Exp57](experiments/Exp57/Exp57.md) | **Success** | Full pipeline: real PS/2 trackpoint → ATtiny85 bridge (Exp56, unchanged) → I2C @0x42 → stock driver → host cursor moves. Flash known-good corne_trackpoint central build (`Exp49`-based poweron uf2). | **Cursor moves with the nub — zero code changes anywhere.** GH Actions run `31527783428` (2m25s) built `corne_trackpoint_right-nice_nano-poweron.uf2` (666,624 B), flashed headless. Live stream via `live-trackpoint.ps1 -Burst` shows real both-axis deltas (e.g. `x=10 y=-15`, `x=-21 y=-15`). One hitch: post-flash USB CDC wedged (shell probes hung in `Open()` even after killing orphans) — unplug+replug fixed it. |
| [Exp58](experiments/Exp58/Exp58.md) | **Success** | Smooth the choppy cursor - two suspects: ZMK per-poll LOG_INF flood on system workqueue, and ATtiny PS/2 `readByte` busy-loop timeout (2000 ~1.5ms) timing out inside the ~7ms inter-packet gap. | ZMK log INF->WRN (Exp58 branch, GH run `31530996047`, flashed COM8): still choppy - reverted (`34a9c73`). ATtiny fix: `setReadTimeout(2000 -> 10000)` (~7.5ms busy-loop, covers the gap) in `trackpoint-i2c-slave-attiny85.ino` - 2440 B, sig `0x1e930b`, verified. **User confirms cursor now smooth.** Choppiness was ATtiny-side; ZMK side net-zero. |
| [Exp59](experiments/Exp59/Exp59.md) | **Success** | Why does the trackpoint "cap" at 31 and stop the mouse on fast flicks? Nothing can display |delta| >= 33 because the ATtiny `max_delta = 32` sanity gate *drops* those packets (last_xraw only stored after the gate), so the debug buffer freezes at 31 = 32-1. ZMK ruled out (raw readout bypasses it). Sensor ruled out (PS/2 is 9-bit signed, ±255/packet). | Fix: `max_delta = 32 -> 127` in `PS2Trackpoint.h`; misalignment already covered by the Exp43 `read_timeouts` mismatch check. Applied to both repos: `attiny85-trackpoint` (Exp59 `1779327`, built 2440 B, flashed + verified via Leonardo ISP, sig `0x1e930b`) and `promini-trackpoint` (Exp59 `3b5322e`, also raised sketch `MAX_DELTA 25 -> 127`; **flashed + verified**, sig `0x1e950f`, 3588 B). **Live-verified on NiceNano COM8**: `live-trackpoint.ps1 -Burst` now streams deltas > 31 (`-48`, `40`, `37`, `-32`) that the old gate discarded. |
| [Exp60](experiments/Exp60/Exp60.md) | **Success** | RawAccel-style **Power** curve baked into the Pro Mini (`f(v)=start+(rate·v)^exp`, whole mode) fixes fast-at-all-speeds feel of the custom flat-top cap: slow nudge < 1 gain (precise crawl), fast flick > 1. Curve in new `PowerCurve` library; params tuned via I2C regs 0x11/0x13/0x15/0x17 written by driver at init from DT props (ZMK-only rebuild to tune). | **Speed finally changes.** Root cause: params were written once at init but the power-gated Pro Mini boots after init + is power-cycled on every deep-sleep wake → RAM params lost → always ran at 1:1 default (also explains Exp53-55 speed tuning doing nothing). Fix: driver re-applies params on first successful poll + every link-restore (`767a5dd`). Final tuning `speed-scale 128` = normal (user-confirmed). Known-good: promini `8e345cb`, driver `767a5dd`, config `4b70d66`. Details in [Exp60](experiments/Exp60/Exp60.md). |
| [Exp61](experiments/Exp61/Exp61.md) | **Success** | Port the Exp60 Power curve to the ATtiny85 I2C slave: copy `PowerCurve` into `attiny85-trackpoint/libraries/`, wire `.ino` (`receiveEvent` 2-byte dispatch for 0x11/0x13/0x15/0x17, `curve.begin()`/`update()`/`apply()`). Same driver + overlay params land automatically → identical curve feel to the Exp60 Pro Mini, zero ZMK changes. | Ported, built, flashed + verified via Leonardo ISP (sig `0x1e930b`, **5576 B** flash/68.1%, **229 B** RAM/44.7%). Kept Exp43 destructive-read burst + debug 0x03; no power-gating/sleep (standalone 4-wire config, user choice). Known-good: attiny85-trackpoint `01d8d24` (Exp59 branch, pushed). **Bench verified** on dabase_v2 right half in place of the Pro Mini: `0x42` responds, cursor moves, curve feel matches Exp60 (`speed-scale 128`, `curve-rate 18`, `curve-exp 256`, `curve-start 77`). Zero ZMK/driver changes. |
| [Exp64](experiments/Exp64/Exp64.md) | **Success** | MOT as active-low data-ready level (PB1→P0.06) replaces the 10ms poll; 1s heartbeat keeps Exp62 recovery + Exp60 param re-apply. Strip unused NPN/PNP gates (keep EXT_VCC P0.13), drop poweroff build. | **Working great.** ATtiny: `set_mot()` in requestEvent/loop, 5956 B flashed + verified (sig `0x1e930b`; note `-D` no-erase fails verify, drop it). Driver: single-shot `read_work` + `heartbeat_work`, unconditional init probe/params, per-read INF→DBG. Overlay: `irq-gpios` P0.06, EXT_POWER=P0.13 only. GH run `31701898644` success, flashed `dabase_v2_right-promini-i2c.uf2` (547840 B) via COM7. Targeted `i2c read 0x42 0x12 2` = clean `00 00`. Known-good: attiny `519e6d8`, driver `84920415`, config `623cddb`. |
| [Exp65](experiments/Exp65/Exp65.md) | **Failed** | ESB split transport (`badjeff/zmk-feature-split-esb`) replaces BLE split on dongle↔left/right — USB-only dongle topology, 1ms min latency vs BLE. Trackpoint/Pro-Mini/driver untouched. | Keystrokes work over ESB. **Chatter never captured** — logs show clean keystrokes, but when chatter occurs log output freezes (no scroll-flood evidence; a suspected layer-6 flood was seen once, not reproduced). ESB is unofficial + buggy; user won't use it — dead end, do not build on ESB. |
| [Exp66](experiments/Exp66/Exp66.md) | **Success** | Enable `CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y` (HID Resolution Multiplier) + retune `zip_scroll_scaler 1 100` to `1 10` for sub-detent scroll precision. Hosts that honor the multiplier interpret wheel counts as 1/16 detent (~3 px) instead of ~50 px, giving 1 px granularity with controlled top speed. | Confirmed: smooth scrolling gives 1 px precision at low speed with controllable top end. `1 10` is a good starting point. Config `75397a0`. |
| [Exp67](experiments/Exp67/Exp67.md) | **Success** | Cursor felt "heavy" at Windows speed 10 (best at 15 = 2.25× OS multiplier). Move that compensation into the keyboard: double the AVR PowerCurve sens via driver DT prop — `speed-scale 128 → 255` (≈×2.0, same curve shape). No driver/ATtiny changes (curve was already on the ATtiny, remotely programmed). | **Working great, user-confirmed at Windows speed 10.** GH run `33073689572`, flashed `dabase_v2_right-promini-i2c.uf2` (547840 B). Config-only: `dabase_v2_right.overlay` one-liner (commit `1dd3c95`, Exp67 branch). Shell probe gotcha: COM3/COM4 are BT SPP (hang on open); NiceNano = `1D50:615E` CDC, shell on COM22. `zip_xy_scaler` rejected — split-topology scaler would need replicating per-central; AVR-side curve is topology-independent. |
| [Exp68](experiments/Exp68/Exp68.md) | **Success** | badjeff's PS/2 driver (`kb_zmk_ps2_mouse_trackpoint_driver`) in a new `NO_HOST_COMMANDS` mode — trackpoint wired **DIRECT** to the nice_nano (DAT P0.08 / CLK P0.06, no ATtiny/Pro Mini). TP streams on power-up and rejects all commands (Exp06), so strip every host write; use the **gpio-ps2** backend (CLK-edge sampling = the AVR mechanism) — the UART trick's fixed-rate sampling can't handle this TP's jittery clock. | **Smooth 4-directional cursor in production-grade config, user says "Perfect"** — zero host commands, zero coprocessor. Final: driver fork `Magid-William/zmk-ps2-trackpoint-driver` (Exp68 `5ef6699`, base = justinmklam-verified 2df4d6d), config `Magid-William/zmk-config-ps2-test` (Exp68 `fb9e917`, run `33206064201`, ZMK pinned `ac7f75b8`, divisor 1). Journey: EXT_POWER = power fix; CLK pull-up; uart-ps2 @9600→14400 (cell-matched) still 50/50 erratic (jittery RC clock) → **gpio-ps2 = smooth**. Bench wiring REVERSED vs stock dabao (DAT on P0.08). |
